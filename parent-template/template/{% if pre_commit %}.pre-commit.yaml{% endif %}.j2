repos:
  # General
  - repo: local
    hooks:
      - id: forbidden-files
        name: forbidden files
        entry: found forbidden files; remove them
        language: fail
        files: "\\.rej$"

[% if python %]
  - repo: https://github.com/psf/black
    rev: 22.12.0
    hooks:
      - id: black
[% endif %]